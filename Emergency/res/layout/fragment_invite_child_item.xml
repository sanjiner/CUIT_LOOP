<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/selector_listview_child_bg"
    android:padding="5dp"
    android:paddingTop="6.0dip" >

    <ImageView
        android:id="@+id/invite_child_icon"
        android:layout_width="50.0dip"
        android:layout_height="50.0dip"
        android:layout_alignParentLeft="true"
        android:layout_marginLeft="5.0dip"
        android:src="@drawable/friend_head"
        android:contentDescription="@string/app_name" />

    <ImageView
        android:id="@+id/invite_child_mode_icon"
        android:layout_width="15dp"
        android:layout_height="15dp"
        android:layout_alignBottom="@id/invite_child_icon"
        android:layout_alignRight="@id/invite_child_icon"
        android:duplicateParentState="true"
        android:src="@drawable/status_online"
        android:contentDescription="@string/app_name" />

    <TextView
        android:id="@+id/invite_child_name"
        android:layout_width="wrap_content"
        android:layout_height="25.0dip"
        android:layout_marginLeft="10.0dip"
        android:layout_marginRight="18.0dip"
        android:layout_toRightOf="@id/invite_child_icon"
        android:duplicateParentState="true"
        android:ellipsize="end"
        android:gravity="left|center"
        android:includeFontPadding="false"
        android:padding="0.0dip"
        android:singleLine="true"
        android:textSize="16.0sp"
        android:text="first" />

    <TextView
        android:id="@+id/invite_child_mode"
        android:layout_width="wrap_content"
        android:layout_height="25.0dip"
        android:layout_below="@id/invite_child_name"
        android:layout_marginLeft="10.0dip"
        android:layout_marginRight="18.0dip"
        android:duplicateParentState="true"
        android:ellipsize="end"
        android:layout_toRightOf="@id/invite_child_icon"
        android:gravity="left|center"
        android:includeFontPadding="false"
        android:padding="0.0dip"
        android:singleLine="true"
        android:textSize="12.0sp"
        android:text="second" />
    
    <CheckBox
        android:id="@+id/invite_checkbox"
       	android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentRight="true"
        android:focusable="false"
        android:focusableInTouchMode="false"
        android:clickable="false" />

</RelativeLayout>